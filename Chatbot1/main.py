# Defines a small main function so this file has a clear entry point when executed directly.
def main():
    # Prints a startup message; this is a simple placeholder and does not launch the Streamlit app.
    print("Hello from chat-with-multiple-pdf-documents!")


# Checks whether this file is being run directly instead of imported by another Python file.
if __name__ == "__main__":
    # Calls the main function so the placeholder message is printed only during direct execution.
    main()
